[build]
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

```

✅ Your folder now has everything needed. It should look like this:
```
wealthdelulu-site/
  ├── index.html
  ├── package.json
  ├── vite.config.js
  ├── netlify.toml
  └── src/
       ├── App.jsx
       └── main.jsx